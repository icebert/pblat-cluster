version: 2
jobs:
  build:
    docker:
      - image: gcc:8.2
    steps:
      - checkout
      - run: sudo apt-get update
      - run: sudo apt-get install -y mpich zlib1g-dev libssl-dev
      - run: make
      - run: ./pblat-cluster

