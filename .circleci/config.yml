version: 2
jobs:
  build:
    docker:
      - image: gcc:8.2
    steps:
      - checkout
      - run: apt-get update
      - run: apt-get install -f -y mpich zlib1g-dev libssl-dev
      - run: make
      - run: ./pblat-cluster

